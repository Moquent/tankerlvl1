class Ball {
  constructor(x,y) {
    var options ={
        frictionAir: 0,
        friction: 0,
        frictionStatic: 1,
        inertia: Infinity,
        restitution: 1.0
    }
    
  }

  display() {
    //Image(this.image)
  }
}
